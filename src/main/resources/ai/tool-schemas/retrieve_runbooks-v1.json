{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "query": {
      "type": "string",
      "description": "Short search phrase based on the incident symptoms and evidence."
    },
    "max_results": {
      "type": "integer",
      "minimum": 1,
      "maximum": 4,
      "description": "Maximum number of runbook chunks to return."
    }
  },
  "required": ["query", "max_results"]
}
